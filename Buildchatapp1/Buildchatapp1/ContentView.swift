import SwiftUI

struct ContentView: View {
    var body: some View {
        ChatListView() // Это то, что будет отображаться на стартовом экране
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
